<nav class="bg-red-500 h-12 flex items-center justify-between px-4 md:px-8">
    <div class="flex items-center gap-1">
        <a class="px-4 py-2 rounded-md text-white font-medium transition-all duration-200 <%= pageTitle === "Index Page" ? "bg-red-600 shadow-md" : "hover:bg-red-600 hover:shadow-sm" %>" href="/">airbnb</a>
        <% if(typeof isLoggedIn !== 'undefined' && isLoggedIn) { %>
            <% if(user && user.userType === 'guest') { %>
                <a class="px-4 py-2 rounded-md text-white font-medium transition-all duration-200 <%= pageTitle === "Home" ? "bg-red-600 shadow-md" : "hover:bg-red-600 hover:shadow-sm" %>" href="/homeList">
                    Homes-List
                </a>
                <a class="px-4 py-2 rounded-md text-white font-medium transition-all duration-200 <%= pageTitle === "Favourite List" ? "bg-red-600 shadow-md" : "hover:bg-red-600 hover:shadow-sm" %>" href="/favouriteList">
                    Favourite List
                </a>
                <a class="px-4 py-2 rounded-md text-white font-medium transition-all duration-200 <%= pageTitle === "Bookings" ? "bg-red-600 shadow-md" : "hover:bg-red-600 hover:shadow-sm" %>" href="/bookings">
                    Bookings
                </a>
            <%} else { %>
                <a class="px-4 py-2 rounded-md text-white font-medium transition-all duration-200 <%= pageTitle === "Host Home List" ? "bg-red-600 shadow-md" : "hover:bg-red-600 hover:shadow-sm" %>" href="/host/hostHomeList">
                    Host-List
                </a>
                <a class="px-4 py-2 rounded-md text-white font-medium transition-all duration-200 <%= pageTitle === "Add Home" ? "bg-red-600 shadow-md" : "hover:bg-red-600 hover:shadow-sm" %>" href="/host/add-home">
                    Add Home
                </a>
            <%}%>
        <% } %>
    </div>
    <div class="flex items-center gap-1">
        <% if(typeof isLoggedIn !== 'undefined' && isLoggedIn) { %>
        <form action="/auth/logout" method="POST" class="inline">
            <button type="submit" class="px-4 py-2 rounded-md text-white font-medium transition-all duration-200 hover:bg-red-600 hover:shadow-sm">Logout</button>
        </form>
        <% } else { %>
        <a class="px-4 py-2 rounded-md text-white font-medium transition-all duration-200 <%= pageTitle === "Login" ? "bg-red-600 shadow-md" : "hover:bg-red-600 hover:shadow-sm" %>" href="/auth/login">Login</a>
        <a class="px-4 py-2 rounded-md text-white font-medium transition-all duration-200 <%= pageTitle === "Sign Up" ? "bg-red-600 shadow-md" : "hover:bg-red-600 hover:shadow-sm" %>" href="/auth/signup">Sign Up</a>
        <% } %>
    </div>
</nav>